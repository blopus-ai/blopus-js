// Single source of truth for the package version. Bump on every release
// (see PUBLISH.md). It also feeds the User-Agent header — a correct value here
// is what keeps Cloudflare from 1010-blocking requests as an unidentified lib.
export const VERSION = "0.3.7";
